[project]
name = "ona-bridge-agent"
version = "0.1.0"
description = "Minimal ONA bridge experiment for toy fit/coreference reasoning"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
